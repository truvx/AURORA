interface:
  display_name: "CI Fix"
  short_description: "Diagnose and fix failing GitHub Actions checks"
  default_prompt: "Use $ci-fix to diagnose the failing checks and make only the authorized minimal fix."
